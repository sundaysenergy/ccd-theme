<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="shortcut icon" href="../../docs-assets/ico/favicon.png">

    <title>{{ page.title }}</title>

    <link href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.css" rel="stylesheet">
    <link href='http://fonts.googleapis.com/css?family=Droid+Serif:400,700,400italic' rel='stylesheet' type='text/css'>
    <link href="/css/genuinerepresentation.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
    <![endif]-->
    <style>
    body {
      padding-top: 0;
      font-family: 'Times New Roman', serif;
      font-size: 1.7em;
      line-height: 1.5;
    }
    h1,h2,h3,h4,h5,h6 {
      font-family: 'Droid Serif';
    }
    .navbar-default {
      background-color: transparent;
      border: none;
    }
    .navbar-default .navbar-nav>li>a,
    .navbar-default .navbar-nav>li>a:hover,
    .navbar-default .navbar-nav>li>a:focus {
      color: #fff;
    }
    </style>
  </head>

  <body>
    <div class="container-fluid">
      <div class="row" style="background: url(/media/bg.jpg); padding-top: 60px;">
        <div class="col-sm-8 col-sm-offset-1">
          <h1>Center for Collaborative Democracy</h1>
        </div>
        <div class="col-sm-2">
<!--           <form class="navbar-form navbar-left" role="form" id="gsearch">
          <div class="input-group">
            <input type="text" placeholder="Search" class="form-control">
            <span class="input-group-btn">
              <button class="btn btn-default" type="submit"><i class="fa fa-search"></i></button>
            </span>
          </div>
          </form> -->
        </div>
      </div>

      <div class="row" style="background: url(http://wbpreview.com/previews/WB0002M7M/images/bg-slider.jpg)">
        <div class="col-sm-10 col-sm-offset-1">
          <nav class="navbar navbar-default" role="navigation">
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
              <ul class="nav navbar-nav">
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown">Programs <b class="caret"></b></a>
                  <ul class="dropdown-menu">
                    <li><a href="#">Forum for Long-Term Prosperity</a></li>
                    <li><a href="#">New Ways to Solve Community Problems</a></li>
                    <li><a href="#">How Any Legislature Can Reach Consensus</a></li>
                    <li><a href="#">Fixing America's Broken Government</a></li>
                    <li><a href="#">How Companies Can Make Better Decisions</a></li>
                  </ul>
                </li>
                <li class="active"><a href="#">About</a></li>
                <li><a href="#">Get Involved</a></li>
                <li><a href="#">Contact Us</a></li>
              </ul>
              <form class="navbar-form navbar-right" role="search" id="gsearch">
                <div class="form-group">
                  <input type="text" class="form-control" placeholder="Search">
                </div>
                <button type="submit" class="btn btn-default"><i class="fa fa-search"></i></button>
              </form>
            </div><!-- /.navbar-collapse -->
          </nav>
        </div>
      </div>
      <div class="row middle">
        <div class="col-sm-5 col-sm-offset-1">
          <img src="http://1.bp.blogspot.com/-ZJYNERNT8z0/UD2AQhcueII/AAAAAAAAAfk/uxL6NpFY4I0/s1600/wave_usa_flagMiddletown+Insider.jpg" class="img-responsive">
        </div>
        <div class="col-sm-5">
          <div class="row">
            <div class="col-sm-12 content{{# page.post_class }} {{ page.post_class }}{{/ page.post_class }}">
              {{{ page.content }}}
            </div>
          </div>
        </div>
      </div>

      <div class="row">
        <div class="col-md-12 lightbluebar">
          Copyright 2013 by the Center for Collaborative Democracy
        </div>
      </div>
    </div>

    <script src="https://code.jquery.com/jquery-1.10.2.min.js"></script>
    <script src="//netdna.bootstrapcdn.com/bootstrap/3.0.2/js/bootstrap.min.js"></script>
    <script>
      $(document).ready(function() {
        $('#gsearch').on('submit', function(e) {
          e.preventDefault();
          if ($('#gsearch input[type=text]').val().length > 0) {
            window.location = '/search/?q=' + encodeURIComponent($('#gsearch input[type=text]').val());
          }
        });
        $('.menu > ul > li').each(function(index) {
          var menu_item = $(this);
          var current_path = window.location.pathname.replace("index.html", ''),
              menu_path = $(this).find('a').attr('href').replace("index.html", '');
          var re = new RegExp(menu_path, 'g');
          if (current_path.match(re) != null) {
            menu_item.find('ul').removeClass('collapse');
            if (menu_item.hasClass('menu-collapsible')) {
              menu_item.removeClass('menu-collapsible').addClass('menu-collapsed').css('list-style-type', 'none');
            }
          }
        });
        $('li').each(function(index) {
          var current_path = window.location.pathname.replace("index.html", '').replace(/[\/]$/, '');
          var link = '';
          if (typeof($(this).find('a').attr('href')) != 'undefined') {
            link = $(this).find('a').attr('href').replace("index.html", '').replace(/[\/]$/, '');
          }
          if (current_path == link) {
            $(this).children('a').addClass('active');
          }
        });
      });
    </script>
    <!-- CAPE.io is buggy at best. -->
  </body>
</html>
