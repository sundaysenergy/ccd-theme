<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="shortcut icon" href="../../docs-assets/ico/favicon.png">

    <title>{{ page.title }}</title>

    <link href="//netdna.bootstrapcdn.com/bootstrap/3.0.1/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/genuinerepresentation.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
    <![endif]-->
  </head>

  <body>
    <div class="container-fluid">
      <div class="row-fluid">
        <div class="col-md-12 menubar">
        </div>
      </div>
      <div class="row-fluid">
        <div class="col-md-12 bluebar">
          <div class="row">
            <div class="col-md-8 col-md-offset-2">
              Center for Collaborative Democracy
            </div>
          </div>
        </div>
      </div>
      <div class="row-fluid middle">
        <div class="col-md-8 col-md-offset-2">
          <div class="row">
            <div class="col-md-3 menu">
              <ul class="navigation">
                {{#data.navigation}}
                <li><a href="{{path}}" title="{{title}}"{{#active}} class="active"{{/active}}>{{title}}</a>
                    {{#has_children}}
                      <ul class="collapse">
                        {{#children}}
                          <li><a href="{{path}}" title="{{title}}"{{#active}} class="active"{{/active}}>{{title}}</a>
                        {{/children}}
                      </ul>
                    {{/has_children}}
                </li>
                {{/data.navigation}}
              </ul>
            </div><!--./col-md-3--> 
            <div class="col-md-9 content{{# page.post_class }} {{ page.post_class }}{{/ page.post_class }}">
              {{{ page.content }}}
            </div>
          </div>
        </div>
      </div>

      <div class="row-fluid">
        <div class="col-md-12 lightbluebar">
          Copyright 2013 by the Center for Collaborative Democracy
        </div>
      </div>
    </div>

    <script src="https://code.jquery.com/jquery-1.10.2.min.js"></script>
    <script src="//netdna.bootstrapcdn.com/bootstrap/3.0.1/js/bootstrap.min.js"></script>
    <script>
      $(document).ready(function() {
        $('.menu > ul > li').each(function(index) {
          var current_path = window.location.pathname,
              menu_path = $(this).find('a').attr('href');
          console.log(current_path, menu_path);
          var re = new RegExp(menu_path, 'g');
          console.log(re);
          console.log(current_path.match(re));
          if (current_path.match(re) && current_path != "/" && current_path != "/index.html") {
            $(this).siblings('ul').removeClass('collapse');
          }
        });
      });
    </script>
  </body>
</html>